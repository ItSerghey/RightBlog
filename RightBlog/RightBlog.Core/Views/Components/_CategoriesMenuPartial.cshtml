@model List<Category>
<ul class="navbar-nav mx-auto">
    @{foreach (var item in Model)
        {
            <li class="nav-item">
                <a class="nav-link" href="@item.Url">@item.Title</a>
            </li>
        }}
</ul>